// Runtime API configuration for HF Static Space deployment.
// Edit ONLY this file to point to a new backend — no rebuild needed.
// After deploying to Render, replace the placeholder with your actual Render URL.
window.APP_CONFIG = {
  API_BASE_URL: "REPLACE_WITH_YOUR_RENDER_URL/api"
  // Example: "https://ddi-xai-backend.onrender.com/api"
};
