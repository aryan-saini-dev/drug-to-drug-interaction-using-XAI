# ── API server runtime dependencies ────────────────────────────────────────────
# torch is installed separately in render-build.sh (CPU-only wheel)
# to avoid accidentally pulling the CUDA version from PyPI.

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.7.0
python-multipart>=0.0.9

# Data science (CPU-only — no GPU needed for inference)
numpy>=1.24.0,<2.6.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# LangGraph pipeline (includes langchain-core automatically)
langgraph>=0.2.0

# Gemini API (gemini_drug_resolver.py uses urllib directly — no SDK needed)
# google-genai is NOT required

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
cloudpickle>=3.0.0
